public interface IShape {
// interface contain methods without implementation
    // all methods are abstract
    double area();

    double perimeter();



    //could be set abstract but it defaults
}
